'use strict';

/**
 * Production environment config of any configuration required to setup
 * the application which includes db, etc for developer purposes.
 */
const defaults = require('./defaults');

module.exports = Object.assign({}, defaults, {});
